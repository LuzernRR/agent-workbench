[project]
name = "agent-workbench-contract-tests"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "jsonschema[format]==4.25.1",
]

[dependency-groups]
dev = [
  "pytest==8.4.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["."]
