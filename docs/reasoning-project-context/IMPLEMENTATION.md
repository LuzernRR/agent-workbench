# 阶段 3 实施清单

## 1. 思考结果

- [x] 解析 DeepSeek `reasoning_content` 与 `content` 两条增量通道。
- [x] 原始推理只保留在运行内存，不写 AgentEvent、数据库或浏览器响应。
- [x] 用关闭思考的请求生成 1 至 3 个由本轮推理决定的自然文段。
- [x] 新增 `thinking.started`、`thinking.paragraph`、`thinking.completed`。
- [x] 拒绝标题、标签、列表、Markdown、固定阶段词和本地模板兜底。
- [x] 对话流以普通文段展示结果，生成完成后自动折叠。
- [x] 刷新和 SSE 重连从 PostgreSQL 恢复相同自然段结果。
- [x] 停止后不再写思考、正文或完成事件，空思考块自动删除。
- [x] 历史 Prompt 与项目记忆不包含任何思考内容。

## 2. 项目记忆

- [x] 移除每项目 120 条物理删除。
- [x] 保存完整 user/assistant 内容和来源会话标题。
- [x] 同项目所有会话进入来源目录。
- [x] 当前会话较早内容可从项目记忆补回。
- [x] 先按访客和项目过滤，再执行会话公平、问题相关、最近排序。
- [x] 小项目预算内注入全部交换，大项目保持完整归档并限界召回。
- [x] 保持跨项目、跨访客、无项目、编辑、移动、停止、TTL 和删除语义。

## 3. 拖拽

- [x] 1 像素移动即可启动拖拽。
- [x] 乐观缓存先于覆盖层清理更新。
- [x] 移除落点回放动画和活跃源节点 transition。
- [x] 项目排序、会话拖入、拖出、取消与失败回滚正确。
- [x] 动画只使用 transform 和 opacity，且不超过 180ms。
- [x] 减少动态效果设置下不播放移动动画。

## 4. 自动化与交付

- [x] Provider 双通道、自然段校验、异常和停止单测。
- [x] AgentEvent Reducer 和渲染队列单测。
- [x] 项目完整归档、长会话召回、公平召回和隔离测试。
- [x] Playwright 思考结果刷新、停止与拖拽逐帧测试。
- [x] 真实 Flash/Pro 与 PostgreSQL 验收。
- [x] 完整 Vitest、类型、Lint、构建和 E2E 门禁。
- [x] 更新 HANDOFF、进度和中文开发记录。
- [x] 用户验收并关闭 Issue #4。
