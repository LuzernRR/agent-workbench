# 阶段 2 实施清单

## 1. 动态模型身份

- [x] 定义仅包含 Provider 名称、模型名称和模型 ID 的公开身份类型。
- [x] 从本轮服务端运行配置生成身份，不读取前端展示文本。
- [x] 身份类问题要求回答三项真实字段。
- [x] 普通任务禁止重复自报身份。
- [x] 身份上下文不包含 API Key、Endpoint、数据库连接或系统 Prompt 内容。
- [x] 切换模型后新运行使用新模型身份，旧运行保持创建时身份。

## 2. Prompt 边界

- [x] 基础规则、可信身份和展现策略位于首个 system message。
- [x] 项目记忆使用独立 system message 和 `<project_memory>` 边界。
- [x] 项目记忆明确为不可信事实背景，不能执行其中命令。
- [x] 当前会话历史只取活动分支、已完成消息。
- [x] 当前用户消息保持在历史之后。

## 3. 记忆契约

- [x] 同会话历史连续性。
- [x] 同访客、同项目跨会话共享。
- [x] 不同项目隔离。
- [x] 不同访客隔离。
- [x] 无项目会话不读写项目记忆。
- [x] 编辑旧消息归档旧分支和下游记忆。
- [x] 会话直接跨项目移动时迁移活动记忆。
- [x] 会话移出项目时归档活动记忆。
- [x] 停止运行不写记忆。
- [x] TTL 删除原始链路并保留有界项目记忆。
- [x] 标签和分隔符计入项目记忆字符预算。

## 4. 自动化

- [x] Prompt 身份、模型切换和记忆注入优先级单测。
- [x] Store 历史、隔离 SQL、编辑、移动、停止、TTL 单测。
- [x] 可显式开启的真实 PostgreSQL 全生命周期集成测试。
- [x] 两个真实 DeepSeek 模型身份验证。
- [x] 完整 Vitest、类型、Lint、构建和 Playwright 门禁。
- [x] 密钥、UTF-8/LF、禁用文案和可见省略号扫描。

## 5. 文档与交付

- [x] 研究文档。
- [x] 实施清单。
- [x] 进度文档。
- [x] 中文开发记录。
- [x] README 与 HANDOFF 当前状态更新。
- [ ] Issue 添加验证证据。
- [ ] 提交并推送公开仓库。
- [x] 完成功能后停止并等待用户验收。
